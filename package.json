{
  "name": "gulp-style-semicolon",
  "version": "1.0.0",
  "description": "Adds a final semicolon to style attributes if none exists.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "plugin-error": "^1.0.1",
    "through2": "^2.0.3"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-mocha": "^5.0.0"
  }
}
